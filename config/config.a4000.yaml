# This machine's per-instance overrides. See config/config.example.machine.yaml
# for the full template and layering explanation.
#
# Only instance differs from the shared config/config.yaml here: gpu ("2,3")
# and model.id (qwen3:30b) in the shared file already match what this box
# actually runs, so they are left there rather than duplicated - see that
# file's comments for why the gpu value is a snapshot, not a reservation.
backend:
  # Friendly name reported by GET /health.
  instance: ubi
